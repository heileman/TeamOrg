<%= form_for(@addresses_person) do |f| %>
  <% if @addresses_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@addresses_person.errors.count, "error") %> prohibited this addresses_person from being saved:</h2>

      <ul>
      <% @addresses_person.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.text_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :address_id %><br />
    <%= f.text_field :address_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
